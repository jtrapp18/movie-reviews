[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "movie-reviews"
version = "0.1.0"
description = "Movie reviews Flask backend"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
