"""
This module intentionally left blank.

The application previously defined an Article model. The backend and frontend
now use Review for both reviews and articles (via content_type), so the
Article model has been removed for consistency and to reduce duplication.
"""
